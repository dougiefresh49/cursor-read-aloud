# Task: Tauri v2 panel scaffold + cards UI + WS client (GitHub issue #19, P4.2)

Read docs/plan-details-phase3-4.md §4.5/§4.7 + file table (rev 3) — your spec. Rust stable 1.96.1 is installed (PATH: export PATH="$HOME/.cargo/bin:/opt/homebrew/bin:$PATH"). pnpm for everything.

Deliverables — new top-level panel/ directory (nothing outside it except .gitignore additions):
1. Tauri v2 app: scaffold manually (don't use create-tauri-app interactive): panel/package.json (vite + typescript + @tauri-apps/cli + @tauri-apps/api), panel/index.html, panel/src/main.ts, panel/src/style.css, panel/src-tauri/ (Cargo.toml with tauri v2, tauri.conf.json: window 360x220, decorations false, alwaysOnTop true, resizable false, title "Room", identifier com.dougiefresh.room-panel).
2. One Rust command in src-tauri/src/lib.rs: `ws_token()` → reads ~/.cursor/tts/panel_ws_token, returns {token, port} (port from ~/.cursor/tts/config.json panel_port, default 4780). ~15 lines with dirs/home expansion.
3. Frontend (vanilla TS, ~250 lines): connect ws://127.0.0.1:<port>/?token=<token>; render agent cards from snapshot messages: avatar img (src `avatars/tmnt/${character?.toLowerCase() ?? 'default'}/${state === 'speaking' ? 'speaking' : 'idle'}.png`, onerror → hide img show initials circle), name, state badge (colored dot + word: working amber, hand_raised blue + CSS pulse, speaking green, idle grey; muted = strikethrough name), raised chip (✋ when hand_raised), superseded chip when >0; click card → send grant; mousedown/mouseup ≥300ms hold → ptt start/stop; global strip: pause ⏸ / stop ⏹ / replay 🔁 buttons + connection dot (green/red); 2s reconnect loop, cards greyed while disconnected. Drag region: -webkit-app-region: drag on body, no-drag on interactive elements. Dark-mode friendly (it's a floating widget — dark by default is fine).
4. panel/public/avatars/tmnt/ — commit a placeholder default/idle.png + default/speaking.png (solid-color 64px PNGs you generate with a tiny node script or ImageMagick if available — NOT AI generation, placeholders only).
5. scripts/panel.sh — launches built app if present else `cd panel && pnpm tauri dev` (background note in header).
6. Root .gitignore: panel/node_modules, panel/src-tauri/target, panel/dist.

Verify: `cd panel && pnpm install && pnpm tauri build --debug 2>&1 | tail -5` — the Rust compile is the gate (first build downloads crates, may take 10+ min; that's fine). If the build fails on something environmental (missing Xcode CLT etc.) report the exact error instead of fighting it. Do NOT launch the app.
Commit: `feat(panel): Tauri v2 scaffold, agent cards, WS client (refs #19)`
