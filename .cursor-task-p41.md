# Task: state-watch.ts + panel-ws.ts (GitHub issue #18, P4.1)

Read docs/plan-details-phase3-4.md §4.6 (rev 3) — your spec — and CLAUDE.md. Read tts-server/src/state.ts, config.ts, hid.ts, index.ts, voice.ts first (reuse patterns; hid.ts shows the failure-isolation style and character/session resolution helpers).

Deliverables (all in tts-server/):
1. src/state-watch.ts — standalone shared module: chokidar watcher on ~/.cursor/tts/state/ (add/change/unlink), debounced 150ms, notifies subscribers (subscribe(cb) → unsubscribe fn). Also exports buildSnapshot(): AgentView[] = for each state file: { sessionId, name, state, raisedAt, character (via session_voices+getCharacter, null-safe), raisedCount (queue/*-cc-<short>*.json count), supersededCount (played/ files for the session newer than raisedAt — cheap approximation: count is fine at 0 if hard), muted (muted_sessions.json) }. Every callback wrapped; watcher errors logged never thrown.
2. src/panel-ws.ts — `ws` package (pnpm add ws + @types/ws): startPanelWs()/stopPanelWs(); config key panel_port (default 4780, 0 = disabled) in config.ts; bind 127.0.0.1 ONLY; on start write a random 32-hex token to ~/.cursor/tts/panel_ws_token (0600); reject upgrades unless ?token= matches AND Origin header is absent or starts with "tauri://" or "http://localhost"; on connect send {type:"snapshot", agents: buildSnapshot()}; re-send full snapshot on every state-watch notification; incoming messages: validate shape strictly — {type:"grant",sessionId} | {type:"ptt",phase:"start"|"stop",sessionId} | {type:"replay"} | {type:"stop"} | {type:"pause"} — unknown → send {type:"error",code:"bad_message"}; grant/ptt sessionIds must exist in current snapshot else {type:"error",code:"stale_session"}; dispatch via spawn of ~/.cursor/tts/scripts/{grant_floor.sh,ptt.sh,stop.sh,pause.sh} and signal replay (mirror hid.ts dispatch style, non-blocking, error-handled).
3. index.ts: start/stop wiring (panel_port > 0), same pattern as startHid.
4. setup.sh: nothing needed (config migration adds panel_port default — add it to the defaults dict).

Constraints: ZERO live API calls; don't run the daemon; don't touch ~/.cursor/tts. Gates: pnpm exec tsc --noEmit clean. Test: a tiny node script that starts panel-ws on a scratch port with TTS_DIR pointed at a temp dir (env override — state.ts uses config TTS_DIR const; if env override isn't feasible, test validation logic with direct function calls instead), connects with ws client, asserts snapshot + bad_message error. Show results in your report.
Commit: `feat(panel): state-watch module + hardened panel WebSocket (refs #18)`
